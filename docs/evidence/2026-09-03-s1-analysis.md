# S1 统计概率首批验证

日期：2026-09-03；规范：roze-skills、AGENTS.md、project-standards.md 与概率统计 V0.2。
临时构建目录：`C:\Users\xFc\AppData\Local\Temp\roze-ta-s1-20260903`。
工具链：rustc 1.98.0 (88d9e12ae, 2026-08-18)，x86_64-pc-windows-msvc，LLVM 22.1.8。
Yata 固定 v0.7.0 / `5030e2349cedde60b0e367a9de9400d466ff644f`，本批无上游修改。
项目尚未提交 Git；变更文件清单与摘要由 `s1-source-sha256.json` 记录。

## 已执行门禁

| 命令 | 结果 |
| --- | --- |
| `rtk cargo test --workspace --locked` | 236 passed：核心 6、analysis 14、engine 12、MCP 3、Yata 132、Yata 文档 69 |
| `rtk cargo clippy -p roze-ta -p roze-ta-mcp --all-targets --no-deps -- -D warnings` | 自有 crate 无警告；上游 7 条既有风格/生命周期警告保持原样 |
| `rtk cargo fmt -p roze-ta -p roze-ta-mcp -- --check` | 通过 |
| `rtk cargo run -p roze-ta --features schema --example export_schema --locked` | 8 个公共 Schema 导出，新增分析请求、结果及后验产物 |
| `rtk proxy powershell -NoProfile -File scripts/verify-upstream.ps1` | 100 个原始文件 SHA-256 一致 |
| `rtk cargo run -p roze-ta --release --example analysis_budget --locked` | Beta(2,3) 固定种子 42，4096 次反函数采样完成；本机单次约 19.86 ms、170620 checkpoint |

日志：[测试](s1-cargo-test.log)、[clippy](s1-clippy.log)、[release timing](s1-release-timing.log)。
release 耗时仅为本机单次 smoke，不是性能承诺；不包含编译时间，未开展负载/跨平台基准。
依赖来源、features、许可证核对见 [清单](s1-dependencies.json) 与 [依赖说明](../dependencies.md)。

## 独立验证映射

`crates/roze-ta/tests/analysis.rs` 的 14 项用例覆盖：

- [1,2,3,4,5] 手算方差、type 7 分位、偏度/超额峰度、MAD/IQR、截尾/缩尾；t(df=4) 参考临界值。
- 常量/单样本、缺失 y、非有限和数值上界、非法 ddof、未知字段。
- 100→110→121 的收益/累计收益/差分/滞后，普通与稳健 Z-score。
- y=2x 的 OLS、协方差、残差、R²、滚动 Beta；含 ties 的平均秩相关 −1/3。
- 正态中心密度/CDF、t(1)=Cauchy、Beta(2,2) 密度 6x(1−x)、Binomial(4,0.5) 组合计数、经验质量。
- CDF/分位单调与反演，离散退化分布，全部五类固定种子采样；无穷分位与无法达到 f64 精度时明确状态。
- 2/3 命中的 Wilson 独立参考；Beta(1,1)+2胜1负→Beta(3,2)，预测均值 0.6。
- 零事件/全事件仍有非退化区间；重复事件 ID 拒绝，按真实区间排除重叠，未成熟/条件不符分开计数。
- 截止后标签不改变选择/估计；追加未来点的截断回放一致；延迟可知时间不被回写。
- 后验版本/参数/哈希损坏拒绝，推断不早于训练截止，新标签不会被推断入口偷偷消费。
- 资源预算在计算前拒绝；计算期间 checkpoint 取消会返回 cancelled，无成功的部分结果。

MCP 官方 SDK 测试扩展：4 个只读工具发现、描述/分布/采样结果与原生完整 JSON 一致、
同样的结构化参数错误与共享并发限额。旧 V1/V2/快照测试保持通过。

## 落地与回滚

通过临时目录门禁后只复制本轮变更文件；原有文件备份到
`C:\Users\xFc\AppData\Local\Temp\roze-ta-s1-backup-20260903`，manifest 记录原有/新增和前后摘要。
回滚按 manifest 恢复原有文件并移除本轮新增文件，不覆盖其他任务改动。
需求正文、指标扩展清单、Yata 原文与 roze-quant 不在本次写入集合。

## 尚未验收

完整 S1/P1 尚未完成。任意维协方差、回归参数区间、通用时间切分、统计流式快照及拟合标准化产物待做；
S2/S3、Linux、完整黑盒取消/超时压力、长时间负载与发布验收未做。
本批后验拟合对完整去重训练集计算，不提供增量样本消费状态。
统计区间的 IID/分布假设并未因程序测试通过而被数据证明。
