/* auto-generated by NAPI-RS */
/* eslint-disable */

/**
 * A whole, non-negative count of bars: a period, lag, or window length.
 *
 * This is a plain `number` to TypeScript. It is named because the binding
 * validates it at the boundary rather than coercing it: a fractional,
 * negative, or non-finite value is refused with a message naming what was
 * passed, instead of arriving at the indicator as a plausible-looking period.
 * The domain rules (a period of zero, the maximum window length) still live in
 * the Rust constructor.
 */
export type Count = number
